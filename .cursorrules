# Ad-Studium 学习笔记项目规则

本项目是个人学习指令、笔记与知识库。所有产出需遵循以下格式与结构。

---

## 一、项目定位

- **类型**：学习笔记 / 知识管理
- **目标**：清晰、可检索、易复习的笔记体系
- **原则**：先有结构，再填内容；格式统一，便于后续整理与 AI 辅助

---

## 二、笔记文件规范

### 2.1 文件命名

- 使用**英文或拼音**，单词用连字符：`topic-subtopic.md`
- 示例：`algorithm-sort.md`、`react-hooks.md`、`shuxue-dao-shu.md`
- 避免空格与特殊符号；日期可放在文件名或 frontmatter 中

### 2.2 单篇笔记结构（必须包含）

每篇笔记建议包含以下区块，顺序可微调：

```markdown
# 笔记标题（与文件名对应或更易读）

> 一句话摘要 / 本笔记解决什么问题

- **标签**：`#标签1` `#标签2`
- **日期**：YYYY-MM-DD
- **来源**：课程/书/文章名（可选）

---

## 1. 核心概念 / 定义

（关键术语、一句话定义）

## 2. 要点 / 步骤

（编号列表或小标题，便于速览）

## 3. 细节与推导

（公式、代码、例子、自己的理解）

## 4. 总结 / 与其它知识的联系

（3–5 句归纳；可写「与 XX 的关系」）

## 5. 参考与延伸

（链接、书名、章节）
```

- 至少要有：**标题、摘要/目标、要点、总结**；其它按需增删。

### 2.3 标题层级

- 一级标题 `#`：笔记标题，每篇只有一个
- 二级标题 `##`：大节（如上面 1–5）
- 三级标题 `###`：小节
- 不要跳过层级（例如不要从 `##` 直接到 `####`）

### 2.4 列表与强调

- 要点用 `-` 或 `1.`，保持同一层级风格一致
- 重要术语或结论用 **粗体**
- 需要记忆的用 `行内代码` 或单独「记忆块」小标题

### 2.5 代码与公式

- 代码块必须带语言标识：` ```js`、` ```python` 等
- 行内公式用 `$...$`，块级用 `$$...$$`（若使用支持 LaTeX 的 Markdown）

---

## 三、目录与索引

- 根目录或各主题目录下应有 **README.md 或 INDEX.md**，列出该目录下笔记标题与简短说明
- 格式示例：

```markdown
# 主题名

| 文件名 | 说明 |
|--------|------|
| xxx.md | 一句话说明 |
```

- 新笔记写完后，把条目补充进对应目录的索引

---

## 四、与 AI 协作时的约定

- **生成新笔记**：按「二、笔记文件规范」生成完整结构，再填内容；若只需片段，也保持标题层级与列表风格一致
- **修改已有笔记**：不改变既有文件名与整体结构，除非用户明确要求重组
- **语言**：笔记正文以**中文**为主；术语、代码、命令保持原文（英文等）
- **引用**：若笔记引用课程/书/网页，在「参考与延伸」中注明来源，便于溯源

---

## 五、推荐目录结构示例

```
Ad-Studium/
├── .cursorrules          # 本文件
├── README.md             # 项目说明 + 总索引
├── subjects/             # 按学科/主题
│   ├── math/
│   ├── programming/
│   └── ...
└── templates/            # 可选：笔记模板
    └── note-template.md
```

可按自己习惯调整主题名（如 `courses/`、`topics/`），但保持一层「主题」再下一层「具体笔记」即可。

---

遵守以上规则，便于你本人复习、检索，也便于 Cursor 在项目内做连贯、格式统一的笔记辅助。
